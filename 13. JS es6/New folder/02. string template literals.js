/* 
    - string template literals are allow embed expressions and variables inside a string using backticks (` `) instead of single or double quotes.
*/
const name = "John";
const age = 30;
console.log(`My name is ${name} and I am ${age} years old.`);
