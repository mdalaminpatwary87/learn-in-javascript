/*====================================================================================================================================================
JavaScript conditional logic => Conditional statements are used to perform different actions based on different conditions.
if                           => if to specify a block of code to be executed, if a specified condition is true. 
                                syntax => 
                                        if (condition) {
                                            // block of code to be executed if the condition is true
                                         }
======================================================================================================================================================*/
// Example 01 => if
var a = 1;
var b = 2;
if (a < b){
    console.log(`${a} is smaller than ${b}`); // here executed it beacuse condition is true.
};